# Supabase configuration (PUBLIC values only — never put a secret/service-role key here).
# Copy this file to `.env` and fill in YOUR OWN Supabase project values.
# Get them from your Supabase dashboard:  Settings → API → Project URL & anon (public) key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=